<div id="page-content-wrapper">
  <div class="container-fluid" id="pref-nav">

  <%= render 'layouts/navbar' %>

<legend id='center-legend'>...a little about you...</legend>
  <fieldset id="">
     <%= form_for @deet, html: { multipart: true, class: "form-horizontal"} do |f| %>
      <%= render f %>
      <%= link_to 'Back', new_preference_path, class: 'btn btn-warning' %>
      <%= f.submit 'Next!', class: 'btn btn-warning' %>
    <% end %>
  </fieldset>

  </div>
</div>
